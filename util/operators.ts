export const Operators = Object.freeze({
    "*": (a:number, b:number) => a * b,
    "-": (a:number, b:number) => a - b,
    "+": (a:number, b:number) => a + b,
    "/": (a:number, b:number) => a / b
})